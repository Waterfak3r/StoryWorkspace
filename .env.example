# Keep the local database out of source control. The default is .data/story-workspace.db.
STORY_WORKSPACE_DB_PATH=.data/story-workspace.db

# Optional AI provider configuration for the reviewable draft assistant. Leave blank to disable AI.
AI_BASE_URL=https://api.openai.com/v1
AI_API_KEY=
AI_MODEL=
